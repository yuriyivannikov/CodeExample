using UnityEngine;

public class EasyAnimationComponent : MonoBehaviour
{
    public float AnimationTime = 1f;
    public AnimationCurve Curve;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
